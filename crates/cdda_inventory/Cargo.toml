[package]
name = "cdda_inventory"
version = "0.1.0"
edition = "2021"
description = "Inventory system — item stacks, invlets, binned lookups, and item movement."

[dependencies]
bevy_ecs.workspace = true
bevy_app.workspace = true
bevy_state.workspace = true
cdda_core_types = { path = "../cdda_core_types" }
cdda_components = { path = "../cdda_components" }
cdda_events = { path = "../cdda_events" }
cdda_actor = { path = "../cdda_actor" }
tracing.workspace = true

[dev-dependencies]
cdda_sim = { path = "../cdda_sim" }
