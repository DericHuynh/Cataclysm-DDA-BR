[package]
name = "cdda_item"
version = "0.1.0"
edition = "2021"
description = "Items, inventory, containers, pockets, materials — domain components."

[dependencies]
cdda_core = { path = "../cdda_core" }
bevy_ecs = "0.18"

[dev-dependencies]
cdda_sim = { path = "../cdda_sim" }
