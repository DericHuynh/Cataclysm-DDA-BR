//! # cdda_sim — Simulation layer
//!
//! ECS components, systems, events, and the deterministic tick loop.
//! Depends on bevy_ecs and bevy_state (not full Bevy).

pub mod state;
pub mod test_utils;
