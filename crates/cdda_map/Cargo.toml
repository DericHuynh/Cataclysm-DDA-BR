[package]
name = "cdda_map"
version = "0.1.0"
edition = "2021"
description = "Map storage, spatial index, and tile queries"

[dependencies]
bevy_ecs.workspace = true
bevy_app.workspace = true
cdda_core_types = { path = "../cdda_core_types" }
cdda_components = { path = "../cdda_components" }
