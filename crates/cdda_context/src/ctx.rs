//! Screen state machine — which screen is currently active.
//!
//! Re-exported from `cdda_components::context` so that all crates share
//! one canonical definition without circular dependencies.

pub use cdda_components::context::{ContextStack, Ctx};
