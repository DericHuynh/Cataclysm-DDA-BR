[package]
name = "cdda_context"
version = "0.1.0"
edition = "2021"
description = "Headless context state machine — Ctx, navigation, focus, overlays, and menu components."

[dependencies]
bevy_ecs.workspace = true
bevy_app.workspace = true
bevy_state.workspace = true
cdda_core_types = { path = "../cdda_core_types" }
cdda_components = { path = "../cdda_components" }
cdda_activity = { path = "../cdda_activity" }
cdda_events = { path = "../cdda_events" }
tracing.workspace = true
