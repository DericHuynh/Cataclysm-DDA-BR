[package]
name = "cdda_intent"
version = "0.1.0"
edition = "2021"
description = "String-typed intent vocabulary — the canonical input/output language between systems, UI, AI, and replay."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
