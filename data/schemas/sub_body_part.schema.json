{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SubBodyPartDef",
  "description": "A sub-body-part definition from JSON type `\"sub_body_part\"`.\n\nDefines a sub-division of a body part (e.g. \"left hand\", \"right arm\").",
  "type": "object",
  "required": [
    "id"
  ],
  "properties": {
    "id": {
      "description": "Unique identifier (e.g. \"sub_part_hand_l\").",
      "allOf": [
        {
          "$ref": "#/definitions/DefId"
        }
      ]
    },
    "max_coverage": {
      "description": "Maximum coverage percentage (0-100).",
      "default": null,
      "type": [
        "integer",
        "null"
      ],
      "format": "uint32",
      "minimum": 0.0
    },
    "name": {
      "description": "Display name (e.g. \"left hand\"). Can be missing for copy-from.",
      "default": null,
      "anyOf": [
        {
          "$ref": "#/definitions/LocalizedString"
        },
        {
          "type": "null"
        }
      ]
    },
    "opposite": {
      "description": "Opposite sub-body-part ID (e.g. \"sub_part_hand_r\").",
      "default": null
    },
    "parent": {
      "description": "Parent body part ID (e.g. \"hand_l\"). Can be string or other identifier.",
      "default": null
    },
    "side": {
      "description": "Side (e.g. \"left\", \"right\").",
      "default": null
    }
  },
  "definitions": {
    "DefId": {
      "type": "string"
    },
    "LocalizedString": {
      "description": "A localized string that may contain translations for multiple languages.\n\nIn CDDA JSON, this appears as: ```json { \"str\": \"name\", \"//~\": \"context\" } { \"str_sp\": \"name\" }           // same for singular & plural { \"str_pl\": \"names\" }          // explicit plural ``` Or as a plain string `\"name\"`.\n\nFor Stage 1 we store the raw English text. Full i18n support (extracting translations) is deferred.",
      "anyOf": [
        {
          "description": "A plain, non-localized string.",
          "type": "string"
        },
        {
          "description": "A localized object with optional plural and context.",
          "type": "object",
          "properties": {
            "context": {
              "type": [
                "string",
                "null"
              ]
            },
            "str": {
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "str_pl": {
              "type": [
                "string",
                "null"
              ]
            },
            "str_sp": {
              "type": [
                "string",
                "null"
              ]
            }
          }
        }
      ]
    }
  }
}