{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AddictionTypeDef",
  "description": "An addiction type definition from JSON type `\"addiction_type\"`.\n\nDefines a type of addiction (e.g. \"caffeine\", \"nicotine\", \"alcohol\").",
  "type": "object",
  "required": [
    "id"
  ],
  "properties": {
    "builtin": {
      "description": "Built-in effect name (alternative to effect_on_condition).",
      "default": null,
      "type": [
        "string",
        "null"
      ]
    },
    "craving_morale": {
      "description": "Morale effect ID associated with craving.",
      "default": null,
      "type": [
        "string",
        "null"
      ]
    },
    "description": {
      "description": "Description of the withdrawal effects.",
      "default": null
    },
    "effect_on_condition": {
      "description": "Effect on condition ID for the addiction.",
      "default": null,
      "type": [
        "string",
        "null"
      ]
    },
    "id": {
      "description": "Unique identifier (e.g. \"caffeine\", \"nicotine\").",
      "allOf": [
        {
          "$ref": "#/definitions/DefId"
        }
      ]
    },
    "name": {
      "description": "Display name (e.g. \"Caffeine Withdrawal\").",
      "default": null,
      "anyOf": [
        {
          "$ref": "#/definitions/LocalizedString"
        },
        {
          "type": "null"
        }
      ]
    },
    "type_name": {
      "description": "Name of the type of substance (e.g. \"caffeine\", \"opiates\").",
      "default": null,
      "type": [
        "string",
        "null"
      ]
    }
  },
  "definitions": {
    "DefId": {
      "type": "string"
    },
    "LocalizedString": {
      "description": "A localized string that may contain translations for multiple languages.\n\nIn CDDA JSON, this appears as: ```json { \"str\": \"name\", \"//~\": \"context\" } { \"str_sp\": \"name\" }           // same for singular & plural { \"str_pl\": \"names\" }          // explicit plural ``` Or as a plain string `\"name\"`.\n\nFor Stage 1 we store the raw English text. Full i18n support (extracting translations) is deferred.",
      "anyOf": [
        {
          "description": "A plain, non-localized string.",
          "type": "string"
        },
        {
          "description": "A localized object with optional plural and context.",
          "type": "object",
          "properties": {
            "context": {
              "type": [
                "string",
                "null"
              ]
            },
            "str": {
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "str_pl": {
              "type": [
                "string",
                "null"
              ]
            },
            "str_sp": {
              "type": [
                "string",
                "null"
              ]
            }
          }
        }
      ]
    }
  }
}