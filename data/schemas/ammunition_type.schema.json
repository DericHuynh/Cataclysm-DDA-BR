{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AmmunitionTypeDef",
  "description": "An ammunition_type definition from JSON type `\"ammunition_type\"`.\n\nDefines a category of ammunition (e.g. \"9mm\", \"223\", \"shot\"). Each ammunition type groups together compatible ammo items under a common name.",
  "type": "object",
  "required": [
    "id",
    "name"
  ],
  "properties": {
    "default": {
      "description": "Default ammunition item ID for this type.",
      "default": null,
      "type": [
        "string",
        "null"
      ]
    },
    "id": {
      "description": "Unique identifier (e.g. \"9mm\", \"223\", \"shot\").",
      "allOf": [
        {
          "$ref": "#/definitions/DefId"
        }
      ]
    },
    "name": {
      "description": "Display name (e.g. \"9x19mm Parabellum\", \".223 Remington\", \"shot\").",
      "allOf": [
        {
          "$ref": "#/definitions/LocalizedString"
        }
      ]
    }
  },
  "definitions": {
    "DefId": {
      "type": "string"
    },
    "LocalizedString": {
      "description": "A localized string that may contain translations for multiple languages.\n\nIn CDDA JSON, this appears as: ```json { \"str\": \"name\", \"//~\": \"context\" } { \"str_sp\": \"name\" }           // same for singular & plural { \"str_pl\": \"names\" }          // explicit plural ``` Or as a plain string `\"name\"`.\n\nFor Stage 1 we store the raw English text. Full i18n support (extracting translations) is deferred.",
      "anyOf": [
        {
          "description": "A plain, non-localized string.",
          "type": "string"
        },
        {
          "description": "A localized object with optional plural and context.",
          "type": "object",
          "properties": {
            "context": {
              "type": [
                "string",
                "null"
              ]
            },
            "str": {
              "default": null,
              "type": [
                "string",
                "null"
              ]
            },
            "str_pl": {
              "type": [
                "string",
                "null"
              ]
            },
            "str_sp": {
              "type": [
                "string",
                "null"
              ]
            }
          }
        }
      ]
    }
  }
}