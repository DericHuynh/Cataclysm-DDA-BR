{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "RotatableSymbolDef",
  "description": "A rotatable symbol definition from JSON type `\"rotatable_symbol\"`.\n\nDefines a set of symbols that represent rotations of the same glyph, used for terrain/furniture rotation.",
  "type": "object",
  "required": [
    "tuple"
  ],
  "properties": {
    "tuple": {
      "description": "The tuple of rotated symbols. Length 2 or 4 depending on symmetry.",
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  }
}